fatigue:
  # 신규 플레이어의 기본 최대 피로도입니다.
  default: 2000

  # 플레이어가 가질 수 있는 최대 피로도의 절대 상한입니다.
  # 낚싯대 옵션(options.max-fatigue)으로 늘어난 최대치도 이 값을 넘지 못합니다.
  max: 20000

  이 내용은 기본값인 2000까지 자연회복된다는 말이고 20000만까지 물약으로 회복등과 같이 수동적인 회복으로 올릴 수 있는 최대 양을 뜻함.

  낚싯대의 
      options:
      max-fatigue: 500        # 이 낚싯대를 들고 있으면 최대 피로도 +500
      옵션은 디폴트 2000+500 이라는 말이며, 최대 자연회복 2500까지 회복된다는 뜻임.

위 내용을 수정할 것.

### 피로도 설정

```yaml
fatigue:
  # 플레이어의 기본 최대 피로도입니다.
  # 자연 회복은 기본적으로 이 수치까지만 회복됩니다.
  # (낚싯대 옵션으로 최대 피로도가 증가하면 증가된 수치까지 자연 회복됩니다.)
  default: 2000

  # 플레이어가 가질 수 있는 절대 최대 피로도입니다.
  # 자연 회복으로는 도달할 수 없으며,
  # 피로도 회복 물약, 관리자 명령어 등의 수동 회복만 이 수치까지 회복할 수 있습니다.
  max: 20000
```

### 예시

기본 상태

```
최대 피로도 : 2000

자연 회복 : 최대 2000까지
```

낚싯대

```yaml
options:
  max-fatigue: 500
```

장착 시

```
최대 피로도 : 2500

자연 회복 : 최대 2500까지
```

추가로 피로도 회복 물약 등을 사용할 경우

```
2500

↓

3500

↓

7000

↓

20000 (최대)
```

까지 회복할 수 있다.
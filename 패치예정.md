# 패치 예정 (PATCH PLAN)

> 이 문서는 현재까지 합의된 패치 내용을 정리합니다.
> 출처: 사용자 요청사항 + `deep-research-report.md` 코드 감사 보고서 대조 검증 결과.

---

## 1. 도감 물고기 이름 표시 개선

**현재 문제**
- 도감 GUI(`CollectionGui`)에서 물고기를 id(`cod`, `salmon`...) 그대로 표시

**변경 내용**
- 물고기 id → 디스플레이 네임으로 표시
- 등급 접두사 결합 예정: `[F] 생대구` 형식
- 미발견 물고기는 `???` 유지
- 관련: `RewardService.resolveDisplayName()` fallback 순서 재사용

**수정 파일**: `CollectionGui.java`

---

## 2. 사이즈 랭킹 개편 — 물고기별 랭킹

**현재 문제**
- SIZE 탭이 각 플레이어의 "가장 큰 물고기 1마리" 기준으로 순위 매김 → 개인 최고 기록 위주

**변경 내용**
- **1단계 (물고기 선택 화면)**
  - 사이즈 있는 물고기(사이즈 없는 물고기 제외)를 등급별로 표시
  - **잡은 적 있는 물고기**: 정상 아이콘 → 클릭 시 해당 물고기 랭킹 열람 가능 (플레이어 순위와 해당 물고기 사이즈가 나타남 트로피 표시도 필요)
  - **못 잡은 물고기**: `???` 회색 표기(도감과 동일 패턴) → 클릭해도 랭킹 접근 불가
  - 접근 제한: **자신이 한 번이라도 잡은 물고기만** 랭킹 진입 가능
- **2단계 (물고기 랭킹 화면)**
  - 랭킹 데이터는 서버 전체 공개 (특정 물고기 랭킹 펼치면 전체 순위 표시)
  - `RankingManager.getTopBySize(fishId, count)` 재사용
  - 내 최고 기록 함께 표시

**구현**
- `RankingManager.getCaughtFishIds(UUID playerUuid)` 추가 — 해당 플레이어가 잡아 사이즈 기록이 있는 물고기 ID 집합 반환
- `RankingGui` 2단계 내비게이션 (목록 → 랭킹 → 뒤로가기)
- 미발견 물고기: `GRAY_DYE` + `???` (도감 GUI와 동일 패턴)

**수정 파일**: `RankingManager.java`, `RankingGui.java`

---

## 3. 어망(Net) 시스템 — 신규

**설계 확정**
- 모든 플레이어 기본 보유 / 아이템 아님 / 최대 **100마리**
- 물고기 잡으면 **자동으로 어망 저장** (아이템 지급 없음)
- 꽉 차면 더 이상 낚시 보상 불가 (실패 처리 + 안내 메시지)
- GUI 진입: `/fishing net` (별칭: `어망`)
- **꺼내기 가능**: 어망 GUI에서 물고기 클릭 → 실제 아이템으로 인벤토리 지급 (빈자리 필요)
- **정렬**: 종류 / 사이즈 / 등급 3가지 모드

**신규 파일** (`me.ninesik.fishing.net` 패키지)
- `NetEntry.java` — 어망 개별 물고기 (fishId, size, gradeId, 잡은 시간)
- `NetData.java` — 플레이어별 어망 (최대 100칸, 추가/제거/정렬)
- `NetStorage.java` — `plugins/InMc-Fishing/net/<uuid>.yml` 저장/로드
- `NetManager.java` — 어망 관리자 (addFish/removeFish/load/save/꽉참 판정)
- `NetGui.java` — 어망 GUI (물고기 목록, 정렬 토글, 클릭으로 꺼내기)

**수정 파일**
- `RewardService.java` — 아이템 지급 → 어망 저장으로 변경. 어망 꽉 차면 실패 메시지. `FishCatchEvent`는 계속 발행(도감/랭킹/대회 연동 유지)
- `FishingCommand.java` — `/fishing net` 서브커맨드 + TabCompleter
- `InMcFishing.java` — NetManager 초기화/종료 연동
- `collections.yml` — `net` 설정 섹션 (max-size: 100)

**주의**
- double(더블)로 2마리 잡으면 어망 2칸 차지
- 대어(big-fish)는 1칸 유지
- 도감 등록은 기존대로 유지 (어망은 별도 저장소)

---

## 4. 물고기 이름 등급 표시 — `[F] 생대구`

**변경 내용**
- `RewardService.createItemStack()`: 아이템 displayName을 `[F] 생대구` 형식으로 표시
- `RewardService.resolveDisplayName()`: catch 메시지에서도 `[F] 생대구`로 보이도록 적용
- 어망 GUI / 랭킹 GUI / 도감 GUI 전부 동일 형식 사용
- 등급 알파벳 색상: 기존 등급 색상 체계 재사용
  (F=흰색, E=초록, D=파랑, C=보라, B=금, A=빨강, S=어두운 빨강 등)

**수정 파일**: `RewardService.java`, 관련 GUI들

---

## 5. 도감 등록 버그 수정

**원인**
- `CollectionManager.consumeOneFishItem()`이 기대 아이템을 `createItemStack(fish, 1)`(size=0)으로 생성
- 실제 잡은 물고기 아이템에는 `사이즈: 35.2cm` lore가 있어 `ItemStack.isSimilar()` 비교 실패 → 등록 불가

**수정**
- `isSameFishItem()` 개선: Material + displayName + customModelData 기반 매칭 (사이즈 lore는 비교에서 제외)
- 어망 도입 후: 어망 GUI에서 꺼낸 아이템(사이즈 lore 유지)으로도 도감 등록 가능

**수정 파일**: `CollectionManager.java`

---

## 6. 대회 HUD — 보스바 + 스코어보드

**설계**
- **보스바 (진행 중)**: 진행 중인 대회에 **참가한 플레이어**에게만 표시
  - 대회 이름 + 남은 시간 (`⏱ 12:34 남음` 형식, 1초마다 갱신)
  - 동시 진행 대회 수만큼 표시
- **스코어보드 (진행 중)**: 참가자에게 표시
  ```
  [대회 이름]
  시작: 00:00
  종료: 01:00 (HH:mm)
  ─────────────
  #1 닉네임 — 기록
  #2 닉네임 — 기록
  #3 닉네임 — 기록
  ─────────────
  내 순위: 5위
  내 기록: 123.4
  ```
- 기록 표시는 대회 타입별: GRADE=`점수`, SIZE=`최대 cm`, COUNT=`마리수`

**구현**
- 신규 `TournamentHud` 클래스 (`me.ninesik.fishing.tournament` 패키지)
  - `BukkitRunnable` 20틱 주기 반복
  - 보스바 생성/갱신: `Bukkit.createBossBar()` + `addPlayer()` / 종료 시 제거
  - 스코어보드: `Scoreboard` + `Objective` 사이드바 모드
- `TournamentManager` 연동:
  - 대회 시작 시 HUD 시작, 종료 시 HUD 정리
  - 참가(join) 시 해당 플레이어에게 즉시 표시
  - 퇴장 시 보스바/스코어보드 제거

**신규 파일**: `TournamentHud.java`
**수정 파일**: `TournamentManager.java`

---

## 7. 안정성 보강 (코드 감사 보고서 `deep-research-report.md` 기반)

> 실제 코드 대조 검증 결과, 아래 항목만 아직 미해결 상태로 확인됨.
> 이미 수정된 항목(Collection claimed, onDisable 정리, saveResource, onEnable 분리)은 제외.

### 7-1. 🔴 FishingSession Player → UUID 변경
- **현재**: `private final Player player;` 직접 보관
- **문제**: 메모리 누수 + `/reload` 시 참조 끊김 위험
- **플랜 고정 규칙 위반**: "세션·이벤트는 객체 참조가 아니라 ID를 들고 있다가 사용 시점에 Registry에서 재조회"
- **수정**: `UUID playerId` 저장, 사용 시점에 `Bukkit.getPlayer()` 조회
- **영향**: `FishingSession`, `FishingSessionManager`, `FishingMiniGame`, `FishingListener` 등 조회부 수정

### 7-2. 🟠 MiniGameManager.stopAllGames() 완성
- **현재**: `if (game.isActive(null)) { // TODO }` → 실제 정리 없음, `isActive(null)` 호출도 버그
- **수정**: 활성 게임 순회하며 BossBar 제거 / 스케줄러 취소 / 상태 정리. `isActive()` 시그니처 정상화

### 7-3. 🟠 FishingSession 컬렉션 캡슐화
- `getSequence()` → `Collections.unmodifiableList()` 반환

### 7-4. 🟠 setCurrentIndex() 인덱스 검증
- 음수/초과 값 설정 시 `IllegalArgumentException` 방어

---

## 8. 명령어 개선 — 별칭 수정 + 계정 지급 + 행동 명령어

### 8-1. `/if` 별칭 동작 수정
- **현재**: `plugin.yml` `aliases: [IF]` — 대문자만 등록되어 서버 구현에 따라 `/if` 인식 안 될 수 있음
- **수정**: `aliases: [if, IF]` — 소문자 `if` 명시 등록
- **수정 파일**: `plugin.yml`

### 8-2. `/fishing give` 명령어 신설 (물고기 + 낚싯대 지급)
- **현재**: 물고기/낚싯대 지급 명령어 없음. 낚싯대는 플러그인에서 획득 경로가 전혀 없음 (`rod.yml`에 3종 등록돼 있으나 지급 수단 부재)
- **추가 명령어** (관리자 전용 `infishing.admin`):
  ```
  /fishing give <플레이어> <물고기ID> [수량] [사이즈]   ← 물고기 아이템 지급
  /fishing give rod <플레이어> <낚싯대ID> [수량]         ← 등록된 낚싯대 지급
  ```
- 물고기 지급: `RewardService.createItemStack(fish, amount, size)` 재사용. 사이즈 미지정 시 기본 사이즈
- 낚싯대 지급: `Rod` 모델 기반 아이템 생성 (vanilla → 이름/로어 FISHING_ROD, mmoitems → MMOItems API 조회)
- 실행 결과(성공/실패) 로그 기록
- TabCompleter: 플레이어 이름, 물고기 ID, 낚싯대 ID 자동완성
- **수정 파일**: `FishingCommand.java`, `RewardService.java`(낚싯대 생성 보조), `RodRegistry.java`(조회)

### 8-3. 접근 어려운 기능 명령어 신설
- **의도**: GUI/관리자 기능만 있고 명령어로 빠른 접근이 어려운 부분 제공
- **추가 명령어**:
  ```
  /fishing fish <물고기ID>       → 물고기 상세 정보 (등급/사이즈/트로피/확률)
  /fishing trophy                → 내 트로피 현황 (일반/레어, 달성 목록)
  /fishing list [등급]           → 물고기 목록 (등급 필터)
  /fishing net                   → 어망 GUI (패치예정 항목 3과 통합)
  ```
- 기존 명령어(`collection`, `rank`, `tournament`, `simulate`, `debug`, `info`, `reload`) 유지
- TabCompleter 전체 확장
- **수정 파일**: `FishingCommand.java`

### 8-4. 낚싯대 획득 경로 (요약)
- 현재 기본 바닐라 낚싯대만 `allow-unregistered-vanilla-rod: true` 로 낚시 가능 (보너스 없음)
- 등록 낚싯대(`advanced_rod`, `master_rod` 등)는 8-2의 `/fishing give rod`로만 획득 가능하게 확장
- 향후 어드민이 원하면 상점/보상 연계 별도 논의

---

## 9. 도감 진행도 기반 물고기 정보 공개

- **현재**: 도감 GUI에서 `???`(미발견) → 발견 시 이름/등급만 표시. 물고기 상세 정보 접근 어려움
- **변경**: 도감 GUI 물고기 lore를 **진행도 단계별**로 공개

### 공개 기준 (도감 등록 진행도 기반)

| 단계 | 공개 정보 |
|------|-----------|
| **미발견** | `???` (기존 유지) |
| **발견(잡기만)** | 이름, 등급, 최소/최대 사이즈, 내 최고/최소 기록 |
| **등록 1+** | + 일반 트로피 사이즈 (`avg × trophy-threshold`, 기본 1.5) |
| **등록 50% 이상** | + 레어 트로피 사이즈 (`max × rare-trophy-threshold`, 기본 0.9) |
| **최대 등록(퍼펙트)** | + 잡힐 확률 (등급 선택 확률 × 등급 내 weight 비율) |

### 정보 계산 소스
- 소스: `RollEngine`(등급 선택), `WeightCalculator`(weight 비율), `GradeRegistry`(등급), `collections.yml`(trophy-threshold)
- 확률 표시 예: `[F] 생대구 — 등급 내 12.6%, 전체 4.2%`

### 수정 파일
- `CollectionGui.java` — 물고기 lore 단계별 공개
- 필요 시 `RollEngine`/`WeightCalculator`에서 확률 계산 메서드 노출
- `ConfigManager` 또는 `CollectionRewardService`에서 트로피 임계값 참조

---

## 10. 대회 우승 랭킹 추가

- **현재**: 대회 결과가 랭킹에 기록되지 않음. `RankingEntry`에 대회 입상 기록이 없고, `TournamentManager.finishTournament()`가 보상 지급 후 `entries.clear()`로 결과를 버림
- **변경**: 랭킹 GUI에 **대회 랭킹 탭** 추가 (COLLECTION / SIZE / TROPHY / 대회)

### 데이터 모델 확장
- `RankingEntry`에 입상 카운트 추가:
  - `tournamentWins` (1위)
  - `tournamentSeconds` (2위)
  - `tournamentThirds` (3위)
- `RankingStorage`(ranking.yml)에 필드 추가:
  - `tournament-wins`, `tournament-seconds`, `tournament-thirds`

### 기록 연동 (`TournamentManager.finishTournament()`)
- 대회 종료 시 상위 3명의 `RankingEntry`에 입상 기록 누적
- `RankingManager.updateTournamentResult(uuid, rank)` 메서드 추가 → 카운트 갱신 + 저장

### GUI/명령어
- `RankingGui`에 대회 랭킹 탭 추가 (정렬: 우승 → 2위 → 3위 순)
- `/fishing rank`(랭킹 GUI)에서 접근, TabCompleter 갱신

### 수정 파일
- `RankingEntry.java`, `RankingStorage.java`, `RankingManager.java`
- `TournamentManager.java` (finishTournament 연동)
- `RankingGui.java` (대회 탭), `FishingCommand.java` (필요 시)

### 🟡 DECISION-NEEDED (확정 필요)
- **랭킹 기준**: (A) 단순 입상 횟수(우승>준우승>3위 순) / (B) 포인트 합산(1위 10, 2위 5, 3위 3)
- **기록 범위**: 대회 타입(GRADE/SIZE/COUNT) 구분 없이 전체 통합 / 타입별 분리
- 현재 기본값: **(A) 입상 횟수, 전체 통합** 으로 계획 (확정 전까지 유지)

---

## 우선순위 (구현 순서)

1. 물고기 이름/등급 표시 (RewardService, CollectionGui)
2. 도감 등록 버그 수정 (isSameFishItem 개선)
3. 사이즈 랭킹 개편 (RankingManager.getCaughtFishIds + RankingGui)
4. 어망 시스템 (Net 패키지 + RewardService 연동 + 명령어)
5. 대회 HUD (TournamentHud + TournamentManager 연동)
6. 안정성 보강 (7-1 ~ 7-4)
7. `/if` 별칭 수정 + `/fishing give` (물고기/낚싯대) + 접근 명령어 (8번)
8. 도감 진행도 기반 정보 공개 (9번)
9. 대회 우승 랭킹 (10번)
10. 빌드 + 검증








관리자 추가 질문안

트로피 🏆
물고기의 특정 크기 이상을 낚으면 트로피를 달성을 하는지?
트로피는 물고기별로 1회만 지급되는게 아니라 조건을 달성하는 물고기들은 트로피 물고기가 되는거임 (해당 물고기의 디스플레이 이름이나, 로어 사이즈 옆에 트로피, 레어트로피 표시가 나타나야함)

패치예정.md가 참조하는 deep-research-report.md의 "이미 수정됨" 항목을 실제 코드와 대조 검증하고, 지난 감사 이후 바뀐 14개 파일을 전부 diff해서 새로 짚이는 문제를 찾았습니다.

✅ 패치예정.md의 "이미 수정됨" 주장 — 실제로 맞는지 검증한 결과: 정확함
항목	검증 결과
Collection claimed	CollectionData.getClaimedRewards() (Set) 신설 + CollectionStorage에 claimed-rewards 로드/저장 반영 확인. 실제로 중복 보상 지급 버그가 고쳐짐
onDisable 정리	onDisable()에서 collection/ranking/tournament/fishingService/dependency 순서로 정상 shutdown 확인
saveResource	mmoitems-example.yml 저장 추가 확인
onEnable 분리	loadRegistries(), saveDefaultResources()로 일부 분리됨 (완전한 리팩터링은 아니지만 사실관계는 맞음)

지난번 제가 지적했던 항목 중에서도 실제로 고쳐진 게 있습니다:

settings.allowed-worlds가 죽은 설정값이었는데, 이번에 FishingListener.isAllowedWorld() + ConfigManager.getAllowedWorlds()로 실제 연결됨
대회 랭킹 점수 공식의 DECISION-NEEDED가 slots + perfect*5 + trophy*10 + rareTrophy*25로 구체화됨
/fishing reload가 이제 collectionManager.reload() / tournamentManager.reload()까지 전파 (단, Registry(등급/물고기/낚싯대) 리로드는 여전히 미구현이라고 코드 주석에 정직하게 남아있음 — handleReload() 주석: "Registry 재로드는 아직 미구현")
중복 콘솔 명령 실행 로직이 util/CommandRunner로 통합됨 (RewardService/CollectionRewardService/TournamentManager 3곳 공용)
🔴 새로 발견한 것 — 대회 시스템에 실제 기능 버그 3건

패치예정.md 6번(대회 HUD)/10번(우승 랭킹) 작업을 준비하며 TournamentManager를 건드렸는데, 그 과정에서 좋아진 것도 있지만 아직 계획서와 어긋나는 부분이 남아있습니다.

1. 대회 자동 종료는 이번에 새로 생겼지만, 대회 상태 자체가 서버 재시작 시 전부 증발함 (서버 종료시, 대회 상태 저장, 로드할것. 서버 강제종료시, 이벤트 취소)

startTournament()에 Bukkit.getScheduler().runTaskLater(..., durationTicks)로 자동 종료가 새로 추가된 건 좋은 개선입니다 (이전엔 대회가 영원히 안 끝났음).
하지만 TournamentStorage.java 파일 자체가 프로젝트에 존재하지 않습니다. 계획서 12.10절이 요구하는 tournaments/active.yml, history/<id>.yml이 전혀 구현 안 됨 → 대회 진행 중 서버가 재시작되면 참가자/점수/순위가 전부 사라집니다. startedToday 같은 스케줄 상태도 메모리에만 있어서 재시작하면 리셋됩니다.

2. join()이 계획서와 반대로 동작함

java
if (!tournament.isRunning()) {
    player.sendMessage(ChatColor.RED + "현재 진행 중인 대회가 아닙니다.");
    return false;
}

계획서 12.4: "대회 시작 전에 참여 신청... 대회 중간에 참여 불가(이미 시작된 대회)" — 그런데 실제 코드는 정반대로, 대회가 이미 isRunning()이어야만 참가할 수 있고, 시작 전(예정) 상태에서는 참가 자체가 막힙니다. 사전 등록 개념이 아예 없는 셈입니다. (이번 diff에 포함된 코드는 아니라 기존부터 있던 문제지만, 대회 로직을 만지는 김에 언급합니다.) -----어드민 첨언 : 사전 참여신청 추가 및 도중 참여 가능으로 변경

3. /낚시 대회 퇴장 <대회ID> 명령어가 존재하지 않음 (플레이어가 접속을 종료 해도 대회에서 퇴장 되지 않도록 수정, 오직 명령어로 퇴장 권장)
계획서 12.9절에 명시된 수동 퇴장 명령어가 없고, handleQuit(Player)는 오직 서버 접속 종료(disconnect) 시에만 호출됩니다. 즉 플레이어가 서버에 남은 채로 "이 대회만 그만하고 싶다"고 할 방법이 없습니다. FishingCommand.java, TournamentManager.java 어디에도 leave 서브커맨드가 없습니다.

🟠 사소하지만 짚을 만한 것

4. infishing.user 권한 노드가 선언만 되고 미사용

yaml
permissions:
  infishing.user:
    default: true
  infishing.admin:
    default: op
    children:
      infishing.user: true

plugin.yml에 새로 추가됐는데, 코드 전체에서 infishing.user를 참조하는 곳이 0곳입니다. 모든 명령어가 여전히 infishing.admin만 체크합니다. 일반 유저 권한 체계를 준비하다 만 상태로 보입니다. *일반 유저 권한으로 대회 참가, 도감 확인,등록등을 실시할 것.

5. RewardService의 명령 실행이 CommandRunner로 바뀌며 동작이 미묘하게 변함
기존엔 Texts.apply(raw, ph)만 하고 색상 제거를 안 했는데, 이제 CommandRunner.execute()가 ChatColor.stripColor(raw)를 먼저 적용합니다. 현재 등록된 commands: 값들엔 색상 코드가 없어서 당장 문제는 없지만, 향후 &가 포함된 명령을 넣으면 이전과 다르게 동작한다는 점은 인지하고 있어야 합니다.
(&가 들어간 내용들에 "&"가 표기되는 문제점 발생 해당 문제 해결요망)


지난 1차 감사에서 지적한 항목 중 이번에도 그대로 남아있는 것 (재확인)
MiniGameManager.stopAllGames() — 여전히 // TODO뿐, isActive(null) 그대로 (패치예정 7-2, 아직 미착수 확인)
FishingSession이 여전히 Player 필드 직접 보관, UUID 전환 안 됨 (패치예정 7-1, 아직 미착수 확인)
ValidationReport의 duplicateId/missingMMOItem 카운터 여전히 사용 안 됨
RollService 여전히 죽은 클래스
GUI 프레임워크(ItemEditorGui/FishGui/RodGui/GradeGui) 여전히 없음
리로드 시스템 재 확인 할 것
종합 의견

패치예정.md의 "이미 수정됨" 주장은 검증해보니 전부 사실과 일치했고, 실제로 견고하게 고쳐져 있었습니다 (특히 claimed-rewards 영속화, allowed-worlds 연동은 로드/세이브까지 꼼꼼하게 확인했습니다). 다만 대회 시스템은 이번 패치로 건드리면서 자동 종료 기능이 생겼지만, 영속성(재시작 생존)·조인 시점·수동 퇴장 세 가지가 여전히 계획서와 어긋나 있어 6번(HUD)·10번(우승 랭킹) 작업을 시작하기 전에 먼저 정리하는 게 좋겠습니다. HUD와 랭킹 연동을 지금 상태 위에 얹으면, 서버 재시작 시 HUD가 가리키는 대회 자체가 증발하는 문제까지 얹힐 수 있습니다.
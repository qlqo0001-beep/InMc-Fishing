# 패치 예정 (PATCH PLAN)

# Trophy Fight 시스템 설계

---

# 검토 후 확정된 사항 (요약)

1. **AI 업데이트 주기**: `ai.update-tick` = 20 (매 Tick마다 갱신, Tick 처리 순서와 일치)
2. **Reel Power ↔ Reel State**: 서로 무관함. Reel Power는 Fish Stamina 감소 효율에만 관여
3. **BossBar 표시 방식**: 게이지(색/길이) = Tension 기준, 네임 텍스트 = Distance 숫자 (0이 되면 종료)
4. **릴 조작 입력**: 좌클릭 연타 = 감기 / 클릭 멈춤 = 정지
5. **등급별 난이도 차등**: 물고기 등급 × 트로피 종류(Trophy/Rare Trophy) 조합으로 난이도 차등 적용 (Rare Trophy가 더 어려움)
6. **Stamina 0 이후 처리**: 자동 종료 아님. Fish AI/Power/Resistance는 정지하되, 플레이어가 계속 릴을 감아 Distance를 0까지 줄여야 함 (단, 이 구간은 장력/릴 부담 없는 안전 구간)
7. **Trophy/Rare Trophy 및 물고기 정보는 아이템 PDC에 저장**: `inmcfishing` 네임스페이스로 `fish_id`/`grade_id`/`is_trophy`/`is_rare_trophy`는 개별 키, 나머지 상세 정보는 `fish_snapshot`(JSON) 하나로 저장한다.
8. **트로피 판정은 사전(Pre) 판정으로 변경**: 기존처럼 아이템 지급 후(로어 표시 시점) 판정하는 것이 아니라, RollEngine이 RewardEntry를 생성하는 시점(미니게임 시작 이전)에 미리 Trophy/Rare Trophy 여부를 판정하여 RewardEntry에 포함시킨다. (RewardEntry 구조 확장은 허용됨 — "구현 원칙" 참고)
9. **트로피 임계값은 단일 값 사용**: Fight 전용 임계값을 별도로 두지 않고, 기존 트로피 판정에 쓰이는 임계값(트로피 임계값)을 그대로 공유해서 사용한다.
10. **`pendingRewards` 네이밍 확정**: `FishingMiniGame.pendingRewards`는 그대로 유지, `CollectionData.pendingRewards`/`PendingReward`는 각각 `pendingMilestoneRewards`/`PendingMilestoneReward`로 리네이밍한다.
11. **Fight 시각적 표현은 엔티티 스폰 없이 파티클/HUD로만 처리**: 실제 물고기 Entity를 스폰하지 않고, Fight 상태는 내부적으로만 관리하며 플레이어에게는 파티클과 HUD(BossBar/ActionBar)로만 표현한다.
12. **등급 키는 실제 `grades.yml` 체계(`f, e, d, c, b, a, s`)를 그대로 사용**: 임의의 등급명(COMMON, LEGENDARY 등)을 새로 만들지 않는다.

---

# 2차 검토 피드백 반영 사항 (요약)

13. **릴 입력(오토클리커) 관련 별도 제한 없음**: 오토클리커/매크로로 연타해도 Tension·Reel State가 동시에 빠르게 소모되어 자체적으로 불리해지므로, 입력 방식 자체에 대한 추가 제한(쿨다운 등)은 두지 않는다.
14. **핵심 수식(Stamina/Tension/Distance/Reel State 계산식)은 구현 단계에서 코드/주석으로 명시**: 별도 수식 문서 또는 `CalculationUtil` 클래스에 정리한다.
15. **보상 지급은 기존 어망→인벤토리 로직 재사용**: `RewardService.giveReward()`의 기존 `NetManager.addFish()` → 실패 시 인벤토리 지급 폴백 구조를 Trophy Fight 성공 시에도 그대로 사용한다. 별도 인벤토리 Full 처리 로직을 새로 만들지 않는다.
16. **낚싯대 교체 감지는 기존 `cleanupPlayer()` 메커니즘을 확장해서 사용**: `FishingListener.cleanupPlayer()`는 이미 `PlayerItemHeldEvent`/`PlayerDropItemEvent`/`PlayerQuitEvent`/`PlayerChangedWorldEvent`/`PlayerDeathEvent` 발생 시 활성 미니게임을 CANCEL 처리하고 있다. Trophy Fight도 이 메서드가 활성 Fight 세션을 함께 종료(실패)하도록 확장한다. 새로운 감지 로직을 별도로 만들 필요가 없다.
17. **Tick당 계산은 그대로, 렌더링(파티클/사운드)만 주기 분리**: Stamina/Tension/Distance/Reel State 등 핵심 수치 계산과 HUD 텍스트 갱신은 매 틱 그대로 수행하되, Particle/Sound 출력만 설정된 인터벌(기본 2~3틱)마다 한 번씩만 실행한다. Queue 기반 처리 방식은 채택하지 않는다.
18. **Fish AI는 상태 기계(State Machine)로 구현**: 각 행동(휴식/이동/돌진/발악 등)을 독립된 상태로 두고, 상태별 지속시간과 전이 확률을 Config(YML)에서 조정 가능하도록 한다. (단, 상태 전이 판정 자체는 기존 확정사항대로 매 틱 수행)
19. **BossBar 표시 방식 재확인**: 게이지 = Tension, 텍스트 = Distance. 기존 확정 내용과 동일하며 변경 없음.
20. **Fight 시작 시 플레이어 이동 완전 구속**: 이동 속도 0, 점프 불가로 고정하고, Fight 종료(성공/실패/취소) 시 즉시 해제한다. (신규 확정 — 아래 "플레이어 이동 제한" 섹션 참고)
21. **`pendingRewards` 리네이밍은 데이터 마이그레이션 불필요로 확정**: 실제 저장 포맷(`CollectionStorage`)의 YAML 키는 `"pending-rewards"`라는 문자열 리터럴이며 Java 필드/클래스명과 무관하다. Java 쪽 필드/클래스명만 `pendingMilestoneRewards`/`PendingMilestoneReward`로 변경하고, YAML 키 문자열은 그대로 유지하면 기존 저장 데이터를 별도 마이그레이션 없이 그대로 읽을 수 있다.
22. **PDC 조회 우선순위 재확인**: `is_trophy`/`is_rare_trophy` 개별 키를 우선 참조하고, `fish_snapshot` JSON은 상세/백업 정보 용도로만 사용한다. 기존 확정 내용과 동일하며 변경 없음.
23. **Stamina 0 이후에는 제한 시간 카운트다운도 함께 정지**: 기존 "자동 종료 아님, Distance만 남아있으면 계속 릴을 감아야 함" 결정에 추가로, Stamina 0 시점부터는 `max-time` 카운트다운도 일시 정지(Pause)하여 시간 초과로 인한 실패 가능성을 원천 차단한다.
24. **Fight 세션 활성 중 신규 입질 이벤트 차단**: `FishingListener.onPlayerFish()`가 기존에 `miniGameManager.hasActiveGame(player)`를 확인해 중복 진입을 막는 것과 동일한 방식으로, Trophy Fight 세션 활성 여부도 함께 확인하여 신규 캐스팅/입질을 무시한다.
25. **RewardEntry의 `isTrophy`/`isRareTrophy` 필드는 기본값 `false`, Nullable 처리**: 필드를 사용하지 않는 기존 코드 경로에는 영향이 없도록 하위 호환성을 보장한다.
26. **테스트용 인게임 명령어 추가**: `/fishing testfight <grade> <trophyType>` (`grade`: f/e/d/c/b/a/s, `trophyType`: trophy/rare). 미니게임을 생략하고 즉시 해당 조건의 RewardEntry를 생성하여 Trophy Fight를 시작한다. (아래 "테스트 및 디버깅 가이드" 섹션 참고)

---

# 개요

Trophy Fight는 **트로피(Trophy) 및 레어 트로피(Rare Trophy) 물고기 전용 힘겨루기 시스템**이다.

일반 물고기는 기존 InMc-Fishing의 일반 미니게임을 성공하면 즉시 획득한다.

반면, Trophy 및 Rare Trophy 물고기는 일반 미니게임 성공 이후에도 추가적인 Fight를 진행해야만 최종적으로 획득할 수 있다.

Trophy Fight의 목적은 단순히 플레이 시간을 늘리는 것이 아니라,

**희귀하고 가치 있는 물고기를 낚았을 때만 경험할 수 있는 긴장감과 성취감을 제공하는 것**이다.

플레이어는 단순히 빠르게 클릭하는 것이 아니라,

물고기의 행동을 읽고,
줄 장력을 관리하며,
릴을 적절한 타이밍에 사용하는

실제 대형어 파이팅(Fighting)을 최대한 게임으로 재현하는 것을 목표로 한다.

---

# 시스템 진입 조건

Trophy Fight는 아래 조건을 모두 만족하는 경우에만 시작된다.

1. 플레이어가 일반 낚시를 진행한다.
2. 입질이 발생한다.
3. 일반 미니게임을 성공한다.
4. RollEngine이 RewardEntry를 생성한다.
5. 생성된 RewardEntry가 Trophy 또는 Rare Trophy이다.

즉,

Trophy Fight는 새로운 보상을 생성하는 시스템이 아니다.

이미 생성되어 pendingRewards에 저장된 RewardEntry를 플레이어가 최종적으로 획득하기 위한 마지막 과정이다.

RollEngine의 확률/보상 생성 로직 자체는 변경하지 않는다.

**※ 단, RewardEntry에는 Trophy/Rare Trophy 여부 필드가 새로 추가되어야 하며, 이 판정은 RewardEntry 생성 시점(RollEngine 직후)에 미리 이루어진다.** (자세한 내용은 "트로피 사전 판정" 섹션 참고)

---

# 전체 진행 흐름

```text
낚시 시작
    │
    ▼
입질 발생
    │
    ▼
RollEngine
    │
    ▼
RewardEntry 생성
    │
    ▼
pendingRewards 저장
    │
    ▼
일반 미니게임
    │
 ┌──┴────────────┐
 │               │
실패            성공
 │               │
Reward 폐기      ▼
          Trophy 여부 확인
                 │
      ┌──────────┴──────────┐
      │                     │
 일반 물고기          Trophy / Rare Trophy
      │                     │
 즉시 지급          Trophy Fight 시작
                            │
                    ┌───────┴────────┐
                    │                │
                  성공             실패
                    │                │
           Reward 지급      Reward 폐기
```

---

# Trophy Fight의 의미

Fight가 시작되었다는 것은

이미 물고기가 낚싯바늘에 걸려 있는 상태를 의미한다.

플레이어는 더 이상 물고기를 낚는 것이 아니라,

도망가려는 물고기를 끝까지 제압하여 자신의 앞으로 끌어오는 과정을 플레이한다.

따라서 Trophy Fight는

- 입질 판정
- 보상 생성
- 확률 계산

과는 완전히 독립된 시스템이다.

Fight는 RewardEntry를 생성하거나 수정하지 않으며,

이미 생성된 RewardEntry를 지급할지 폐기할지만 결정한다.

---

# 게임 목표

플레이어의 목표는

물고기를 안전하게 제압하여 회수하는 것이다.

이를 위해서는

- 물고기의 행동을 관찰하고
- 장력을 관리하며
- 릴을 감을 타이밍을 판단하고
- 무리하지 않고 기다릴 줄도 알아야 한다.

계속 릴만 감는다고 성공하는 구조는 허용하지 않는다.

Fight는

물고기를 먼저 지치게 만든 뒤,

저항이 약해졌을 때 안전하게 회수하는 방식으로 진행된다.

---

# 핵심 시스템

Trophy Fight는 아래 시스템이 서로 영향을 주며 진행된다.

---

# Fish AI

Fish AI는 물고기의 현재 행동을 결정한다.

물고기는 Fight 동안 항상 같은 행동을 반복하지 않는다.

현재 상황과 남은 체력에 따라

- 천천히 이동
- 일반 이동
- 강하게 돌진
- 방향 전환
- 잠시 휴식
- 마지막 발악

등 다양한 행동을 수행한다.

AI는 일정한 랜덤성을 가지며,

항상 동일한 순서로 행동해서는 안 된다.

AI가 현재 선택한 행동은

Fight 중 Fish Power를 지속적으로 변화시킨다.

**※ 확정: Fish AI는 상태 기계(State Machine)로 구현한다.** 위에 나열된 각 행동(천천히 이동/일반 이동/강한 돌진/방향 전환/휴식/마지막 발악)을 독립된 상태로 정의하고, 상태별 지속 시간과 다음 상태로의 전이 확률을 Config(YML)에서 조정 가능하도록 한다. 단, 상태 전이 여부 판정 자체는 "Tick 처리" 섹션에 명시된 대로 매 틱 수행한다(틱마다 반드시 새 행동으로 바뀌는 것은 아니며, 현재 상태의 지속 시간이 끝났는지를 매 틱 확인한다).

---

# Fish Power

Fish Power는

현재 물고기가 순간적으로 내고 있는 힘이다.

Fight 중 가장 자주 변하는 능력치이며,

Fish AI에 의해 계속 변화한다.

예를 들어

- 휴식
- 천천히 이동
- 일반 이동
- 강한 돌진
- 마지막 발악

마다 서로 다른 Power를 가진다.

Power가 높을수록

- 플레이어와의 거리를 더 빠르게 벌린다.
- 줄 장력이 더 빠르게 증가한다.
- 릴 상태에 더 큰 부담을 준다.

Power는 물고기의 순간적인 힘을 의미하며,

Fight 중 계속 변화한다.

---

# Fish Resistance

Fish Resistance는

물고기가 플레이어에게 끌려오지 않으려는 저항력이다.

Power와 달리

순간적인 힘이 아니라

지속적으로 버티는 능력을 의미한다.

Resistance가 높으면

- Distance 감소량이 줄어든다.
- 릴을 감아도 거의 끌려오지 않는다.
- 릴 상태가 더 빠르게 감소한다.

Resistance는 Fight 동안 계속 변하며,

Fish Stamina가 감소할수록 함께 감소한다.

---

# Fish Stamina

Fish Stamina는

물고기의 체력이다.

Fight는 단순히 거리를 줄이는 게임이 아니라,

물고기의 체력을 먼저 소모시키는 과정이다.

플레이어가 적절한 타이밍에 릴을 사용할수록

Fish Stamina는 더욱 빠르게 감소한다.

Stamina가 감소할수록

- Fish Power가 감소한다.
- Fish Resistance가 감소한다.
- 강한 행동 빈도가 감소한다.
- 휴식 행동이 증가한다.

Stamina가 0이 되면

물고기는 더 이상 저항하지 않는다.

이후

Fish AI는 움직이지 않으며,

Power와 Resistance는 모두 0이 된다.

플레이어는 장력 증가 없이

물고기를 안전하게 회수할 수 있다.

---

# Distance

Distance는

플레이어와 물고기 사이의 거리이다.

Fight의 최종 목적은

Distance를 0까지 줄이는 것이다.

Distance는

- 플레이어가 릴을 감으면 감소한다.
- 물고기가 도망가면 증가한다.

단,

Distance 감소량은

현재 Fish Resistance의 영향을 받는다.

Resistance가 높을수록

같은 릴 조작으로도

거리가 거의 줄어들지 않는다.

반대로

물고기가 충분히 지쳐 Resistance가 낮아지면

Distance는 빠르게 감소한다.

Distance가 0이 되면

물고기를 회수하게 된다.

---

# Tension

Tension은

현재 낚싯줄에 걸리는 장력이다.

Fight에서 가장 중요한 위험 요소이다.

플레이어가

Fish Power가 높은 상태에서

무리하게 릴을 감으면

장력이 급격하게 증가한다.

장력이 최대치에 도달하면

낚싯줄이 끊어지고

Fight는 즉시 실패한다.

반대로

릴을 잠시 멈추면

장력은 서서히 감소한다.

Line Strength가 높을수록

최대 장력이 증가하여

더 높은 장력을 견딜 수 있다.

---

# Reel State (릴 상태)

릴 상태는

Fight 동안 릴이 얼마나 버틸 수 있는지를 나타내는 내구도이다.

쉽게 말해

릴의 HP라고 볼 수 있다.

릴은 Fight 동안

물고기의 힘과 저항을 계속 받아 부담이 누적된다.

특히

- Fish Power가 높을수록
- Fish Resistance가 높을수록
- 무리하게 릴을 사용할수록

릴 상태가 빠르게 감소한다.

릴 상태가 낮아질수록

릴 효율이 감소하며,

물고기의 Stamina를 소모시키는 효율도 함께 감소한다.

반대로

잠시 릴을 멈추면

릴 상태는 천천히 회복된다.

릴 상태가 0이 되면

릴이 한계를 넘어 고장 나며

Fight는 즉시 실패한다.

Reel Durability가 높을수록

릴 상태 감소 속도가 느려져

오랫동안 안정적으로 Fight를 진행할 수 있다.

---

# Trophy Fight 스탯 구성

Fight에서 사용되는 핵심 스탯은 다음과 같다.

## 물고기

- Fish Stamina
- Fish Power
- Fish Resistance

## 낚싯대

- Reel Power
- Line Strength
- Reel Durability

각 스탯은 서로 독립적으로 존재하지 않으며,

Fight 동안 유기적으로 서로 영향을 주고받는다.

---

# 낚싯대 스탯

플레이어가 사용하는 낚싯대는 Fight 동안 아래 세 가지 능력치를 가진다.

각 능력치는 서로 다른 역할을 담당하며,

모든 능력치는 Fight에 직접적인 영향을 준다.

---

## Reel Power

Reel Power는

릴이 물고기를 얼마나 효율적으로 제압하는지를 나타내는 능력치이다.

Reel Power가 높다고 해서

물고기를 강제로 끌어오는 것은 아니다.

대신

릴을 감을 때 물고기의 체력을 더욱 효과적으로 소모시킨다.

즉,

Reel Power는

Distance를 줄이는 능력이 아니라,

**Fish Stamina를 감소시키는 능력**이다.

Reel Power가 높을수록

- Fish Stamina 감소량 증가
- Fight 시간이 단축됨
- 물고기를 더 빠르게 지치게 만들 수 있음

하지만

Fish Power가 매우 높은 상태에서

무리하게 릴을 감으면

Reel Power가 높더라도

장력과 릴 상태에는 그대로 부담이 발생한다.

**※ 확정: Reel Power는 Reel State(릴 내구도) 감소량에 전혀 영향을 주지 않는다.**

Reel State 감소는 오직 Fish Power, Fish Resistance, Reel Durability에 의해서만 결정되며, Reel Power는 순수하게 Fish Stamina 감소 효율에만 관여하는 독립적인 스탯이다.

---

## Line Strength

Line Strength는

낚싯줄의 강도이다.

줄이 버틸 수 있는 최대 장력을 결정한다.

Line Strength가 높을수록

- 최대 Tension 증가
- 높은 Fish Power를 안정적으로 버틸 수 있음
- 강한 돌진에도 줄이 끊어질 위험 감소

하지만

장력이 증가하는 속도를 줄여주는 것은 아니다.

장력을 얼마나 버틸 수 있는지만 결정한다.

---

## Reel Durability

Reel Durability는

릴 자체의 내구도이다.

Fight 동안 릴은

물고기의 힘과 저항을 계속 받아 손상된다.

Reel Durability가 높을수록

- Reel State 감소 속도 감소
- 높은 Fish Power를 오래 버틸 수 있음
- 높은 Fish Resistance를 오래 상대할 수 있음

Reel Durability는

Fight의 안정성을 결정하는 가장 중요한 장비 능력치이다.

---

# 스탯 상호작용

Fight에서는

각 스탯이 서로 독립적으로 동작하지 않는다.

모든 스탯은 서로 영향을 주며

Fight의 난이도를 만들어낸다.

---

## Fish Power ↔ Line Strength

Fish Power는

줄에 순간적인 힘을 전달한다.

Power가 높을수록

Tension이 빠르게 증가한다.

Line Strength가 높을수록

더 높은 장력을 견딜 수 있다.

즉,

높은 Power를 가진 물고기를 상대하기 위해서는

충분한 Line Strength가 필요하다.

---

## Fish Power + Fish Resistance ↔ Reel Durability

Fish Power는

릴에 순간적인 부담을 준다.

Fish Resistance는

릴에 지속적인 부담을 준다.

두 능력치가 동시에 높을 경우

Reel State가 빠르게 감소한다.

Reel Durability가 높을수록

이 부담을 더욱 오래 견딜 수 있다.

---

## Reel Power ↔ Fish Stamina

Reel Power는

Distance를 직접 줄이지 않는다.

대신

릴을 감을 때

Fish Stamina를 감소시킨다.

Stamina가 감소하면

- Fish Power 감소
- Fish Resistance 감소

결국

물고기가 점점 지치면서

Distance를 줄이기 쉬워진다.

---

## Fish Resistance ↔ Distance

Resistance는

플레이어에게 얼마나 끌려오지 않으려 하는지를 나타낸다.

Resistance가 높을수록

Distance 감소량이 크게 줄어든다.

반대로

Resistance가 낮아질수록

같은 릴 조작으로도

Distance를 빠르게 줄일 수 있다.

---

# 전투 흐름

Trophy Fight는

물고기를 억지로 끌어오는 게임이 아니다.

플레이어는

먼저 물고기를 지치게 만든 뒤

안전하게 회수해야 한다.

전투는 아래 순서로 진행된다.

```text
Fish AI 행동 결정
        │
        ▼
Fish Power 변화
        │
        ▼
플레이어 입력
        │
        ▼
Fish Stamina 감소
        │
        ▼
Fish Power 감소
Fish Resistance 감소
        │
        ▼
Distance 감소
        │
        ▼
물고기 회수
```

Fight가 길어질수록

물고기는 점점 지쳐간다.

플레이어는

무리하게 릴을 감는 것이 아니라,

적절한 타이밍에 릴을 사용하여

물고기의 체력을 먼저 소모시키는 것이 중요하다.

---

# 플레이 방식

Fight 동안

플레이어는 계속 릴을 감는 것이 아니다.

현재 물고기의 상태를 확인하면서

- 지금 감아야 하는지
- 지금 기다려야 하는지

를 계속 판단해야 한다.

**※ 확정: 릴 조작 방식**

- **릴 감기**: 좌클릭을 연타하는 방식으로 낚싯줄을 감는다.
- **릴 멈춤**: 클릭을 멈추면 릴 조작이 정지된 것으로 간주한다.

예를 들어

물고기가 강하게 돌진하는 동안

계속 릴을 감으면

- 장력이 급격하게 증가하고
- 릴 상태도 빠르게 감소한다.

반대로

릴을 잠시 멈추면

- 장력이 감소하고
- 릴 상태가 회복된다.

물고기가 휴식하거나

Stamina가 많이 감소한 상태에서는

적극적으로 릴을 사용하여

체력을 빠르게 소모시키는 것이 유리하다.

---

# Fight 진행 단계

Fight는 크게 네 단계로 진행된다.

### 1단계 : 초기 힘겨루기

Fight 시작 직후

물고기는 충분한 체력을 가지고 있다.

Power와 Resistance가 모두 높다.

플레이어는

무리하게 릴을 감기보다

물고기의 행동을 관찰하는 것이 중요하다.

---

### 2단계 : 체력 소모

플레이어가 적절한 타이밍에 릴을 감으면

Fish Stamina가 점차 감소한다.

Stamina가 감소할수록

물고기의 행동이 점차 약해진다.

---

### 3단계 : 지침

Fish Power와 Resistance가

점차 감소하기 시작한다.

Distance를 줄이기가 쉬워진다.

플레이어는

이 시점부터 적극적으로 릴을 사용하여

물고기를 회수한다.

---

### 4단계 : 회수

Fish Stamina가 0이 되면

물고기는 더 이상 움직이지 않는다.

Fish AI가 정지하며

Power와 Resistance도 모두 0이 된다.

**※ 확정: Stamina가 0이 되는 순간 Fight가 자동으로 즉시 종료되는 것은 아니다.** Fish AI/Power/Resistance는 모두 멈추지만, Distance가 아직 0이 아니라면 플레이어는 계속 좌클릭으로 릴을 감아 Distance를 0까지 줄여야 한다. 다만 이 구간에서는:

- Fish Power/Resistance가 0이므로 Tension이 더 이상 증가하지 않는다 (장력 위험 없음).
- Reel State도 더 이상 부담을 받지 않는다.
- **제한 시간(max-time) 카운트다운도 이 시점부터 일시 정지(Pause)된다.** 즉, 남은 시간에 관계없이 시간 초과로 실패할 위험이 없다. (자세한 내용은 "제한 시간" 섹션 참고)
- 즉, 실패할 위험 없이 안전하게 릴을 감기만 하면 되는 "마무리 연출" 구간이다.

이후

장력 증가 없이

Distance를 0까지 줄여

안전하게 물고기를 회수한다.

---

# HUD

Fight 동안

BossBar와 ActionBar를 이용하여

실시간 정보를 표시한다.

HUD는

매 Tick 갱신된다.

---

## BossBar

BossBar는

Fight의 진행 상태를 표시한다.

표시 항목

- Distance
- Tension

BossBar 게이지는

현재 Tension을 기준으로 표시하며 (게이지 길이/색상), 네임 텍스트는 Distance 수치로 나타난다.

예)

- 초록 : 안정
- 노랑 : 주의
- 빨강 : 위험

**※ 확정: BossBar 네임에는 "Distance: 62" 형태로 현재 Distance 값을 표시하며, 릴을 감아 Distance가 감소할수록 이 숫자가 점점 줄어들다가 0이 되면 Fight가 종료(회수 성공)된다.** 즉, 게이지(색/길이)는 Tension의 위험도를, 네임 텍스트는 Distance의 진행 상황을 각각 담당하여 두 정보를 동시에 전달한다.

---

## ActionBar

ActionBar는

현재 Fight 상태를 표시한다.

표시 항목

- Fish Stamina
- Fish Power
- Fish Resistance
- Reel State

예시

```
Stamina 65%
Power ▲▲
Resistance ▲
Reel 82%
```

플레이어는

BossBar만 보고도

현재 언제 릴을 감아야 하는지,

언제 기다려야 하는지를 판단할 수 있어야 한다.

---

# 시각 효과

Fight 동안

물고기의 상태에 따라

Particle을 출력한다.

예)

- 강한 돌진 → 큰 물보라
- 휴식 → 작은 물결
- 마지막 발악 → 큰 물보라 + 스플래시

이를 통해

HUD를 보지 않아도

현재 물고기의 행동을 어느 정도 예측할 수 있도록 한다.

---

# 사운드

Fight 동안

물고기의 행동에 따라

사운드를 출력한다.

예)

- 강한 돌진
- 릴 과부하
- 장력 위험
- 물고기 지침
- 회수 성공

모든 사운드는

config.yml에서

활성화 여부 및 종류를 변경할 수 있어야 한다.

---

# Tick 처리

Trophy Fight는 20TPS를 기준으로 동작한다.

Fight 동안 아래 순서를 매 Tick마다 반복 실행한다.

1. Fish AI 업데이트 (상태 기계 전이 판정 포함)
2. Fish Power 계산
3. Fish Resistance 계산
4. Player Input 반영
5. Fish Stamina 계산
6. Distance 계산
7. Tension 계산
8. Reel State 계산
9. HUD 갱신
10. Particle 출력 (설정된 인터벌마다만 실행 — 아래 참고)
11. Sound 출력 (설정된 인터벌마다만 실행 — 아래 참고)
12. 성공 여부 확인
13. 실패 여부 확인

각 단계는 반드시 순서를 유지해야 한다.

예를 들어

Fish AI보다 먼저 Power를 계산하거나,

Stamina보다 먼저 Distance를 계산해서는 안 된다.

Fight의 모든 결과는

현재 Tick에서 계산된 최신 상태를 기준으로 처리한다.

**※ 확정: 성능 최적화를 위해 렌더링(Particle/Sound)만 주기를 분리한다.** Stamina/Tension/Distance/Reel State 등 핵심 수치 계산과 HUD 텍스트 갱신(9번)은 매 틱 그대로 수행하되, Particle(10번)과 Sound(11번) 출력만 `particle.interval`/`sound.interval` 설정값(기본 2~3틱)마다 한 번씩 실행한다. 입력 지연을 유발하는 Queue 기반 처리 방식은 채택하지 않는다.

---

# 성공 조건

아래 조건을 모두 만족하면

Fight는 성공한다.

- Fish Stamina ≤ 0
- Distance ≤ 0
- 줄이 끊어지지 않음
- 릴이 파손되지 않음

성공 시

기존 pendingRewards에 저장되어 있는 RewardEntry를 그대로 지급한다.

새로운 Reward를 생성하지 않는다.

Reward의 등급

사이즈

트로피 여부

레어 트로피 여부

등은 Fight와 관계없이

RollEngine에서 이미 결정된 값을 그대로 사용한다.

---

# 실패 조건

아래 조건 중 하나라도 만족하면

Fight는 즉시 종료된다.

- Tension이 최대치 도달
- Reel State가 0 도달
- 낚싯대 아이템이 손에서 사라짐/교체됨 (엔티티 미스폰 방침에 따라 "물고기 Entity 소멸" 대신 이 조건으로 대체)
- 플레이어가 낚시를 취소
- 플레이어 로그아웃
- 플레이어 사망
- 제한 시간 초과

**※ 확정: Fight 중 물고기 Entity를 스폰하지 않으므로("Fight 시각적 표현 방식" 섹션 참고), "낚싯줄이 제거됨" / "물고기 Entity가 사라짐" 조건은 실제 엔티티 소멸 감지가 아니라 위 대체 조건(낚싯대 교체/해제 등)으로 판단한다.**

**※ 확정: 낚싯대 이탈/교체, 로그아웃, 사망, 월드 이동, 아이템 드롭 감지는 새로 만들지 않고 기존 `FishingListener.cleanupPlayer()` 메커니즘을 확장해서 사용한다.** `cleanupPlayer()`는 이미 `PlayerItemHeldEvent`(핫바 변경), `PlayerDropItemEvent`, `PlayerQuitEvent`, `PlayerChangedWorldEvent`, `PlayerDeathEvent` 발생 시 활성 미니게임을 `CANCEL` 처리하고 있다. Trophy Fight Manager도 이 메서드에서 함께 종료(실패) 처리되도록 훅을 추가한다.

실패 시

pendingRewards에서 해당 RewardEntry를 제거한다.

보상은 지급되지 않는다.

---

# 제한 시간

Fight는 무한히 지속되지 않는다.

각 Fight는

설정된 제한 시간을 가진다.

제한 시간이 초과되면

물고기가 도망간 것으로 처리하며

Fight는 실패한다.

제한 시간은

config.yml에서

자유롭게 변경할 수 있어야 한다.

**※ 확정: Fish Stamina가 0이 되는 순간 제한 시간(max-time) 카운트다운은 일시 정지(Pause)된다.** 내부적으로 Timer를 두어 Stamina ≤ 0 조건이 충족되면 카운트다운을 멈춘다. 이 구간은 Tension/Reel State 부담도 없는 완전한 안전 구간이므로, 플레이어가 여유롭게 Distance만 0으로 만들면 되고 시간 초과로 실패할 일이 없다.

---

# 설정 파일

Fight의 모든 수치는

config.yml을 통해 변경 가능해야 한다.

예시

```yaml
trophy-fight:

  enabled: true

  max-time: 120

  tick-rate: 20

  tension:

    max: 100

    recover: 1.5

  reel:

    max-state: 100

    recover: 0.8

  distance:

    start: 100

  stamina:

    base: 100

  ai:

    update-tick: 20

  hud:

    enabled: true

  sound:

    enabled: true

    interval: 3

  particle:

    enabled: true

    interval: 2

  movement-lock:

    enabled: true
```

Fight의 난이도를 결정하는 모든 값은

코드 수정 없이 설정 파일만으로 조정할 수 있어야 한다.

**※ 확정: `ai.update-tick`은 20(=20TPS, 매 Tick)으로 고정한다.** Fish AI는 "Tick 처리" 섹션에 명시된 순서대로 매 Tick마다 행동을 갱신하며, 별도의 저빈도 업데이트 방식은 사용하지 않는다.

**※ 확정: 물고기 등급 및 트로피 종류(Trophy / Rare Trophy)에 따라 난이도가 차등 적용된다.** 자세한 내용은 아래 "등급별 난이도 차등" 섹션 참고.

---

# 등급별 난이도 차등

Trophy Fight의 난이도는 고정값이 아니며,

**물고기 등급 × 트로피 종류(Trophy / Rare Trophy)** 조합에 따라 달라진다.

## 기본 방향

- Trophy는 상대적으로 잘 나오는 편이므로 난이도가 낮거나 중간 수준이다.
- Rare Trophy는 진짜 희귀한 트로피이므로 난이도가 확연히 높아야 한다.

즉, 같은 물고기 등급이라도 Rare Trophy로 판정된 개체는 Trophy로 판정된 개체보다 더 강한 Fight를 요구한다.

## 차등 대상 값

등급 × 트로피 종류 조합에 따라 아래 값들이 달라질 수 있다.

- Fish Stamina 초기값
- Fish Power 범위(최소/최대)
- Fish Resistance 범위(최소/최대)
- AI 행동 빈도(강한 돌진, 마지막 발악 등의 발생 확률)
- 제한 시간(max-time)

## 설정 파일 구조 방향

물고기 등급(실제 InMc-Fishing의 `grades.yml` 등급 체계 — `f, e, d, c, b, a, s`, 소문자로 정규화됨) × 트로피 종류(trophy, rare-trophy) 조합별로 개별 수치를 지정할 수 있도록,

기존 `trophy-fight` config 하나로 통합하기보다는

**등급별로 별도 YML 섹션(또는 별도 파일)을 구성**하는 것을 원칙으로 한다.

**※ 확정: 등급 키는 반드시 실제 `GradeRegistry`/`grades.yml`에 정의된 소문자 등급 ID(`f, e, d, c, b, a, s`)와 동일하게 사용한다.** 별도의 임의 등급명(예: COMMON, LEGENDARY 등)을 새로 만들지 않는다.

예시 구조 (세부 키 이름은 구현 단계에서 확정):

```yaml
trophy-fight:
  enabled: true
  tick-rate: 20
  hud:
    enabled: true
  sound:
    enabled: true
  particle:
    enabled: true

  grades:
    c:
      trophy:
        max-time: 90
        stamina: 80
        power: { min: 10, max: 30 }
        resistance: { min: 10, max: 30 }
      rare-trophy:
        max-time: 150
        stamina: 150
        power: { min: 25, max: 55 }
        resistance: { min: 25, max: 55 }
    s:
      trophy:
        max-time: 120
        stamina: 120
        power: { min: 20, max: 45 }
        resistance: { min: 20, max: 45 }
      rare-trophy:
        max-time: 200
        stamina: 220
        power: { min: 40, max: 80 }
        resistance: { min: 40, max: 80 }
```

tension/reel/distance 등 공통 기본값은 상위 `trophy-fight` 레벨에 두고,

등급 × 트로피 종류별로 달라지는 값(Stamina, Power, Resistance, max-time 등)만 `grades` 하위에서 오버라이드하는 구조로 설계한다.

---

# 트로피 사전 판정 (Pre-Evaluation)

기존에는 트로피 판정(Trophy/Rare Trophy 여부)이 아이템 지급 이후, 로어(lore) 텍스트 표시 목적으로만 이루어졌다.

Trophy Fight 도입에 따라 판정 시점을 아래와 같이 변경한다.

- **판정 시점**: RollEngine이 RewardEntry(사이즈 포함)를 생성한 직후, 미니게임 시작 이전.
- **판정 결과**: RewardEntry에 Trophy 여부, Rare Trophy 여부가 필드로 포함되어 이후 전 과정(미니게임 성공 판정, Trophy Fight 진입 여부 판단, 아이템 로어 표시, PDC 저장)에서 재계산 없이 그대로 참조된다.
- 기존처럼 아이템을 만든 뒤 다시 판정하는 방식은 사용하지 않는다. 판정은 한 번만 수행되고, 그 결과를 모든 하위 시스템이 공유한다.

이를 통해 "미니게임 성공 → Trophy 여부 확인 → Fight 진입 여부 결정" 순서(시스템 진입 조건 참고)가 실제로 성립할 수 있다.

**※ 확정: `RewardEntry`에 추가되는 `isTrophy`/`isRareTrophy` 필드는 기본값 `false`, `@Nullable` 처리한다.** 이 필드를 참조하지 않는 기존 코드 경로(예: 기존 `RewardService.evaluateTrophyType()`을 그대로 쓰는 로어 표시 로직 등)는 영향을 받지 않으며 하위 호환성이 보장된다.

---

# 트로피 임계값 (단일 값 사용)

Trophy Fight 진입 여부를 가르는 임계값은 Fight 전용으로 별도 정의하지 않는다.

기존에 사용되던 **트로피 임계값**(트로피/레어 트로피를 가르는 사이즈 비율 기준)을 그대로 사용하며, 이 값은 아래 두 목적에 동일하게 적용된다.

- 잡은 물고기가 Trophy/Rare Trophy인지 판정 (트로피 사전 판정에서 사용)
- 아이템 로어에 트로피 문구 표시 여부 결정

즉, 트로피 임계값을 조정하면 "이 물고기가 트로피인가"라는 판정 결과 자체가 바뀌며, 그 결과가 Trophy Fight 진입 여부·로어 표시·PDC 저장 값에 모두 동일하게 반영된다. Fight 난이도만 따로 조절하고 싶다면 임계값이 아니라, 등급별 난이도 차등 섹션에서 정의하는 Fight 자체의 수치(Stamina/Power/Resistance 등)를 조정한다.

---

# 아이템 PDC(PersistentDataContainer) 저장

지급되는 물고기 아이템에는 해당 물고기의 정보를 PDC를 통해 전부 저장한다.

## 목적

- 이후 서버 설정(config, fish 정의 등)이 변경되더라도, 이미 지급된 아이템이 지급 당시의 정보를 그대로 유지하도록 한다.
- Trophy Fight, 도감, 랭킹 등 여러 시스템이 아이템만 보고도 필요한 정보를 즉시 조회할 수 있도록 한다.

## 저장 대상

- 물고기 ID, 사용 타입, 소재/이름 등 물고기 정의 정보
- 등급(Grade) 및 원본 등급(originalGrade)
- 사이즈(size), 최소/최대/평균 사이즈
- Double 여부, 대어(BigFish) 여부
- **Trophy 여부, Rare Trophy 여부 (트로피 사전 판정 결과)**
- 커스텀 모델 데이터

## 저장 방식 (확정)

- **네임스페이스**: `inmcfishing`
- **빠른 조회용 개별 키** (파싱 없이 즉시 확인 가능해야 하는 값):
  - `inmcfishing:fish_id` (STRING)
  - `inmcfishing:grade_id` (STRING)
  - `inmcfishing:is_trophy` (BYTE, 0/1)
  - `inmcfishing:is_rare_trophy` (BYTE, 0/1)
- **상세 스냅샷 키** (물고기 정보 전체를 하나로 묶은 직렬화 데이터):
  - `inmcfishing:fish_snapshot` (STRING, JSON 직렬화 — 사이즈, 최소/최대/평균 사이즈, originalGrade, Double 여부, BigFish 여부, 커스텀 모델 데이터 등 나머지 정보 전부 포함)

Trophy Fight 및 다른 시스템(도감, 랭킹 등)은 `is_trophy`/`is_rare_trophy` 키만으로 빠르게 트로피 여부를 판단하고, 상세 정보가 필요할 때만 `fish_snapshot`을 파싱한다.

---

# `pendingRewards` 네이밍 정리 (확정)

기존 코드에 `pendingRewards`라는 이름이 서로 다른 두 가지 개념으로 이미 존재한다.

- `FishingMiniGame.pendingRewards` (`Map<UUID, RewardEntry>`) — Trophy Fight가 사용할 미니게임 성공 후 보상 대기 구조
- `CollectionData.pendingRewards` (`List<PendingReward>`) — 도감 마일스톤 커맨드 보상 대기열 (완전히 다른 개념)

두 개념은 서로 무관하므로, 아래와 같이 이름을 구분하여 혼동을 없앤다.

- **`FishingMiniGame.pendingRewards`는 그대로 유지한다.** Trophy Fight의 핵심 개념(RewardEntry 단위 보상 대기)과 명세상 이름이 정확히 일치하므로 그대로 사용한다.
- **`CollectionData.pendingRewards` / `PendingReward` 클래스는 리네이밍한다.**
  - 필드: `pendingRewards` → `pendingMilestoneRewards`
  - 클래스: `PendingReward` → `PendingMilestoneReward`
  - 관련 메서드(`addPendingReward`, `isPendingExpired` 등)도 `MilestoneReward` 명칭에 맞춰 함께 정리한다.

**※ 확정: 이 리네이밍은 데이터 마이그레이션이 필요 없다.** `CollectionStorage.load()`/`save()`가 실제로 읽고 쓰는 YAML 키는 `"pending-rewards"`라는 문자열 리터럴이며, Java 필드명/클래스명과는 무관하게 독립적으로 정의되어 있다. 따라서 Java 쪽 식별자만 변경하고 YAML 키 문자열(`"pending-rewards"`)은 그대로 유지하면, 기존에 저장된 플레이어 데이터를 별도 변환 로직 없이 그대로 읽고 쓸 수 있다.

---

# Fight 시각적 표현 방식

Trophy Fight 진행 중에도 별도의 물고기 Entity(엔티티)는 스폰하지 않는다.

- Fight의 모든 상태(Fish AI, Power, Resistance, Stamina, Distance, Tension, Reel State)는 내부 데이터로만 관리한다.
- 플레이어에게는 오직 **파티클**과 **HUD(BossBar/ActionBar)** 를 통해서만 현재 상태를 표현한다.
- 즉, 화면상에 물고기가 실제로 보이는 형태가 아니라, 낚싯대를 든 상태에서 파티클과 HUD 정보만으로 Fight를 체감하도록 한다.

---

# 플레이어 이동 제한 (신규)

Trophy Fight가 시작되면 플레이어의 이동을 완전히 구속한다.

- **시작 시**: 이동 속도 0(`walk speed 0`), 점프 불가로 고정한다.
- **종료 시**: 성공/실패/취소(로그아웃·사망 포함) 관계없이 즉시 원래 이동 속도와 점프 가능 상태로 복구한다.
- **예외 처리**: 구속 중 로그아웃/사망 등으로 비정상 종료되는 경우에도 반드시 해제 로직이 실행되도록 한다 (`cleanupPlayer()` 확장 지점 또는 별도 종료 콜백에서 처리).

이동 제한을 두는 이유는, 이동으로 물고기와의 거리(Distance)를 임의로 조작하거나 Fight 밸런스를 깨는 것을 막기 위함이다. `config.yml`의 `movement-lock.enabled`로 활성화 여부를 조정할 수 있다.

---

# Fight 세션 중 중복 액션 차단 (신규)

Trophy Fight 세션이 활성화된 동안에는 신규 낚시 시도(캐스팅)를 차단한다.

- 기존 `FishingListener.onPlayerFish()`가 `miniGameManager.hasActiveGame(player)`를 확인해 일반 미니게임 중복 진입을 막는 것과 동일한 방식으로, Trophy Fight 세션 활성 여부도 함께 확인한다.
- Fight가 활성 중인 플레이어에게 새로운 입질(BITE) 이벤트가 발생하면 조기 리턴하여 무시한다.
- 새로운 리스너를 별도로 만들기보다, 기존 `onPlayerFish()`의 활성 게임 확인 로직에 Trophy Fight 세션 확인을 추가하는 방식으로 구현한다.

---

# 테스트 및 디버깅 가이드 (신규)

## 자동화 단위 테스트

- 각 등급(`f, e, d, c, b, a, s`) × 트로피 종류(`trophy`, `rare-trophy`) 조합별로 가상 시뮬레이션을 실행하여 성공 확률을 산출한다.
- 평균적인 클릭 속도, 무작위 클릭, 최적 클릭 등 다양한 입력 전략에 대한 성공률을 검증하여 밸런스를 확인한다.

## 인게임 테스트 명령어

```
/fishing testfight <grade> <trophyType>
```

- `<grade>`: `f`, `e`, `d`, `c`, `b`, `a`, `s` (소문자 등급 ID, `grades.yml` 체계와 동일)
- `<trophyType>`: `trophy` 또는 `rare`

동작:

- 일반 미니게임을 생략하고, 해당 조건에 맞는 RewardEntry를 즉시 생성하여 바로 Trophy Fight 세션을 시작한다.
- HUD, Fish AI, Particle/Sound 등 모든 요소가 실제 플레이와 동일하게 동작한다.
- Fight 종료(성공/실패) 시 최종 Stamina, 경과 시간 등 상세 수치를 로그로 출력한다.

---

# 시스템 구조

Trophy Fight는

기존 InMc-Fishing 시스템과 최대한 독립적으로 구현한다.

Fight는

Reward 생성 시스템을 수정하지 않는다.

Fight는

Reward를 저장하지 않는다.

Fight는

Reward를 계산하지 않는다.

Fight는

이미 생성되어 있는 RewardEntry를 참조만 한다.

Fight 종료 후

성공이면 지급

실패이면 삭제

두 가지 처리만 수행한다.

---

# 구현 원칙

Trophy Fight는

기존 시스템을 최대한 건드리지 않는 것을 원칙으로 한다.

구현 시 반드시 아래 사항을 지킨다.

- RollEngine의 확률/보상 계산 로직 자체는 수정하지 않는다.
- **단, RewardEntry에는 Trophy Fight 연동을 위한 필드(Trophy 여부, Rare Trophy 여부)를 추가한다.** (RewardEntry 구조 확장은 허용, 확률 계산 로직 변경은 금지)
- 기존 일반 낚시 시스템과 독립적으로 동작
- Fight 전용 Manager 구성
- Fight 데이터는 플레이어 단위로 관리
- 모든 계산은 Tick 기반 처리
- 모든 수치는 Config에서 수정 가능
- 향후 새로운 AI 패턴을 쉽게 추가할 수 있도록 확장 가능한 구조 사용
- 낚싯대 이탈/로그아웃/사망/월드이동 등 세션 종료 감지는 새 리스너를 만들지 않고 기존 `FishingListener.cleanupPlayer()`를 확장해서 사용
- 신규 캐스팅 차단은 기존 `onPlayerFish()`의 활성 게임 확인 로직에 Trophy Fight 세션 확인을 추가하는 방식으로 구현 (별도 리스너 신설 금지)
- `CollectionData.pendingRewards`/`PendingReward` 리네이밍 시 YAML 저장 키(`"pending-rewards"`) 문자열은 변경하지 않아 마이그레이션이 필요 없도록 함

---

# 설계 목표

Trophy Fight는

클릭 속도를 겨루는 미니게임이 아니다.

플레이어가

물고기의 행동을 관찰하고

적절한 타이밍을 판단하며

장력과 릴 상태를 관리하는

전략적인 힘겨루기를 목표로 한다.

Fight는

운만으로도

실력만으로도

성공해서는 안 된다.

좋은 장비와

좋은 판단이 함께 이루어질 때

안정적으로 성공할 수 있어야 한다.

---

# Trophy Fight 핵심 관계

```
Fish AI
      │
      ▼
Fish Power
      │
      ├──────────────┐
      ▼              ▼
Distance 증가    Tension 증가
                      ▲
                      │
              Line Strength

────────────────────────────────

Reel Power
      │
      ▼
Fish Stamina 감소
      │
      ▼
Fish Power 감소
Fish Resistance 감소

────────────────────────────────

Fish Resistance
      │
      ▼
Distance 감소량 결정

────────────────────────────────

Fish Power
      +
Fish Resistance
      │
      ▼
Reel State 감소
      ▲
      │
Reel Durability
```

---

# 플레이 흐름

플레이어는

물고기를 강제로 끌어오는 것이 아니라

먼저 지치게 만든다.

↓

물고기가 지칠수록

Power 감소

Resistance 감소

↓

Distance를 줄이기 쉬워진다.

↓

장력과 릴 상태를 관리하면서

안전하게 회수한다.

---

# 구현 우선순위

## 1단계

- Trophy Fight Manager
- Fight Session
- Tick 처리
- 기본 HUD

---

## 2단계

- Fish AI
- Fish Power
- Fish Resistance
- Fish Stamina

---

## 3단계

- Reel State
- Tension
- Distance 계산

---

## 4단계

- BossBar
- ActionBar
- Sound
- Particle

---

## 5단계

- Config 연동
- 난이도 밸런싱
- 최적화

---

# 향후 확장 가능 요소

현재 Trophy Fight는

세 가지 물고기 스탯과

세 가지 낚싯대 스탯만을 사용한다.

추후 업데이트를 통해

아래 요소를 쉽게 추가할 수 있도록 설계한다.

- 물고기별 AI 패턴
- 물고기 성격(공격적, 신중함 등)
- 날씨에 따른 행동 변화
- 시간대에 따른 행동 변화
- 낚싯대 장비 옵션
- 버프 및 디버프
- 파티 낚시
- 토너먼트 전용 Fight

현재 구조는

확장 가능한 기반 시스템으로 설계하며,

기존 로직을 수정하지 않고

새로운 기능을 추가할 수 있도록 구현한다.

---

# 최종 목표

Trophy Fight의 목표는

희귀한 물고기를 단순히 운으로 획득하는 것이 아니라,

플레이어가

물고기의 움직임을 읽고,

적절한 타이밍에 대응하며,

긴장감 넘치는 힘겨루기를 통해

최종적으로 물고기를 제압하는 경험을 제공하는 것이다.

플레이어는

Fight가 끝난 순간

"힘들었지만 내가 이겼다."

라는 성취감을 느낄 수 있어야 한다.

이것이 Trophy Fight 시스템이 지향하는 최종 목표이다.